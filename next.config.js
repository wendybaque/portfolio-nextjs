/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
        domains:[
            "images.pexels.com",
            "pixabay.com"
        ]
    }
}

module.exports = nextConfig
