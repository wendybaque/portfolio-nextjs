/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains:[
            "images.pexels.com",
            "pixabay.com",
            "devicons.dev",
            "cdn.jsdelivr.net",
            "i.goopics.net"
        ]
    }
}

module.exports = nextConfig
