/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains:[
            "images.pexels.com",
            "pixabay.com",
            "devicons.dev",
            "cdn.jsdelivr.net"
        ]
    }
}

module.exports = nextConfig
